spring.datasource.url: jdbc:${CLEARDB_DATABASE_URL:mysql://localhost:3306/mydiplomdb?useUnicode=true&serverTimezone=UTC}
spring.datasource.username: root
spring.datasource.password: test
server.port: ${PORT:8080}

spring.jpa.hibernate.ddl-auto: none
spring.jpa.show-sql: false

spring.servlet.multipart.enabled: true
spring.servlet.multipart.max-file-size: 25MB
spring.servlet.multipart.max-request-size: 25MB

max.upload.image-size.bytes: 5242880

blog.path: ${BLOG_PATH:http://localhost:8080/}

static.classpath: classpath:/static/
upload.avatars: images/avatars
upload.image: images/posts

avatar.height: 36
avatar.width: 36

settings.positive: 'YES'
settings.negative: 'NO'

captcha.response.attribute: data:image/png;base64,

blog.title: DevPub
blog.subtitle: Рассказы разработчиков
blog.phone: +7 903 666-44-55
blog.email: mail@mail.ru
blog.copyright: Александр Дмитриев
blog.copyrightFrom: все права не защитить. 2005

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation: true

spring.mail.host: smtp.yandex.ru
spring.mail.username: ${MAIL_USERNAME:username}
spring.mail.password: ${MAIL_PASSWORD:password}
spring.mail.port: 465
spring.mail.protocol: smtps

cloudinary.url: ${CLOUDINARY_URL:url}
cloudinary.api.key: ${CLOUDINARY_API_KEY:0000}
cloudinary.api.secret: ${CLOUDINARY_API_SECRET:secret}